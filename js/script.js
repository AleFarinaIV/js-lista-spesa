const ingredients = ['pomodori', 'pasta', 'guanciale', 'pepe nero', 'pesto', 'macinato']

const shoppingList = document.getElementById('shopping-list')

let count = 0

while (count < ingredients.length) {
    let item = document.createElement('li')
}